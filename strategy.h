#include "shuffle.h"
#include "controller.h"
class Strategy {
    public:
    int checkDiceType(std::string s);
};

class Load {
    public:
    int roll_load();
};

class Fair {
    public:
    int roll_fair();
};
